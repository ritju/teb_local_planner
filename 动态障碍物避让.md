# 动态障碍物避让 — 目标说明

## 核心目标

在动态障碍速度不可靠时，优先保证机器人在当前位置附近、短时域内的轨迹对外部交通参与者可预测；具体表现为：禁止在近场短时内出现大幅度转向（大角速度变化）。

## 为什么定这一条目标

### 背景问题

1. 当前 TEB 动态避障主要采用固定权重方法：对障碍物做未来时空轨迹预测，再按动态障碍权重与距离建边优化。
2. 动态障碍物的速度信息可能与实际偏差较大；若强依赖速度预测做长时/整条局部路径上的避障，容易因错误速度产生错误绕行。
3. 仅调大 `weight_dynamic_obstacle` 不能从根上解决问题：权重过小则真冲突时避不开，权重过大则会放大由错误速度引起的过度反应。

### 安全取向（优先于效率）

本功能的核心是**基于安全，而不是效率**。对行走的人或车辆（尤其是快速行驶的车辆），在接近机器人时需要能够预判机器人轨迹。若机器人在有人或车接近时突然大幅度转向，快速车辆可能反应不过来，反而更危险。

因此：

- 成功标准首先是：接近过程中机器人行为可被对方预判；
- 其次才是是否躲开了基于速度的动态预测轨迹；
- 宁可多减速/短暂停，不可因不可靠速度预测触发突然大转向。

### 为何收束为“近场 + 短时 + 可预测”

1. **速度不可靠 → 弱化对动态预测的依赖**  
   恒速时空外推会放大速度误差。动态速度只适合作为弱辅助，不宜作为主导大幅度机动的依据。预测与约束都应主要落在机器人当前位置附近、短时域内，而不是整条局部路径上的轨迹预测。

2. **可预测性是该背景下最合适的安全定义**  
   对快速车辆，真正危险的往往不是“机器人没绕开某条预测轨迹”，而是“机器人突然改航向，对方按旧轨迹预判失效”。把安全定义为近场短时内轨迹可预测，比定义为“尽量高效躲开动态预测包络”更贴切。

3. **用“禁止大幅度转向”作为可操作的具体表现**  
   “突然大幅度转向”定义为：在机器人当前位置附近（距离近 + 时间近），角速度变化较大。它约束的是轨迹执行形态，与静态/动态障碍分类无关：不因动态响应在近场短时内急转，静态避障若需转弯也应提前、平滑完成。

4. **为何不拆成多条独立目标**  
   - “近距风险保底”等场景很难穷举抽象，不宜单独列为易膨胀的第 N 条目标。  
   - “禁止会与动态障碍碰撞的急转”会重新依赖速度预测来判断“会不会撞”，与速度不可靠背景冲突。  
   - 近场几何不碰撞（矩形 footprint，含转向时后轮廓）仍是规划常识底线，手段应是减速/平滑小机动，而不必再升为与核心目标并列的独立条款。

### 目标隐含边界（便于追溯，不升格为并列目标）

- 可预测优先于通行效率；不以提升效率为由，用不可靠速度预测驱动大幅度避障机动。
- 位置/外形通常相对速度更可信：近场仍应保持几何间距，必要时减速；但不拿不可靠速度做长时绕行决策。
- “大幅度转向”的度量对象是近场短时窗内的角速度（或航向变化率），后续实现再落到具体阈值。

---

## 技术方案（现有 TEB 框架内）

### 总体思路

在现有 TEB 内落地核心目标，原则是：

1. **不以** `AddEdgesDynamicObstacles`（恒速时空预测 + 动态权重建边）作为主避障手段。
2. **近场几何**：动态障碍按**当前位置**走静态障碍边（信任位置）。
3. **近场可预测**：用 2c 式约束——近场短时内角速度**保持当前状态附近**，而不是压到 0 或突然改硬上限。
4. **抗抖动**：动态障碍短时缓存 + 门控迟滞，避免断帧导致约束闪烁。

不新造规划器；主要改障碍建边分流、近场角速度保持边、以及动态数据缓存。

### 决策结论

#### 1. 是否采用 `AddEdgesDynamicObstacles`

- **不作为主手段**；可关闭，或权重极低且不用于驱动大幅度绕行。
- 原因：速度不可靠时，全程动态边容易错误拉动轨迹，与“可预测优先”冲突。
- **只做近场禁止大幅度转向，对“可预测”够用，对“别撞上”不够**——因此必须配合下面第 2 点。

#### 2. 不用动态边时，近场如何处理动态障碍

- **已拍板**：近场一律按**静态位置**建障碍边（当前位置 / footprint 距离约束）。
- 窗外（非近场）动态障碍：不以其速度做长时预测拉轨迹；近场以外主要靠静态 costmap / 既有静态逻辑。

#### 3. 线速度与角速度

- **先不动线速度**，只约束角速度；与目标表述（禁止大幅度转向）一致。
- **不做 2a / 2b**：不通过临时全局或分段硬降 `max_vel_theta` 把角速度压小——正在转弯时会突然回正，反而不可预测。
- **采用 2c（`EdgeNearHorizonOmegaHold`）**：见下一节；**不是**把角速度锁死为当前值或优化到 0。

#### 4. 动态障碍数据抖动 / 断帧

- **需要短时缓存**，否则近场门控与角速度保持约束会随断帧忽开忽关，造成轨迹抖动。
- 建议：
  - 缓存最近一次有效动态障碍（id / 位置 / 限幅后可用于短时外推的速度）。
  - 保持时间量级 **0.2–0.5 s**（按感知频率标定）；过长易出“幽灵障碍”。
  - 超时前：仍可参与门控与静态近场建边；超时后：带迟滞平滑退出。
  - 断帧期间速度更不可信，但仍可按缓存做**限幅短时外推**仅用于门控；不作长时绕行。

#### 5. `EdgeNearHorizonOmegaHold` 激活与约束（已拍板）

**不得**对所有运动障碍一律建边；仅当短时威胁门控命中时才启用，避免无关运动体锁转向、干扰前方静态绕障。

**门控（对每个缓存动态障碍）**：

1. **轮廓外推**：以位置 + 膨胀为主；速度用于短时外推障碍轮廓（不是“弱提示”），但速度先夹紧：最大线速度 `v_x_max`、最大角速度 `omega_z_max`，仅在近场时间窗 `T_near` 内推进。
2. **已重叠则排除**：若外推/当前障碍轮廓与**当前机器人轮廓**已相交或过近 → **不建 OmegaHold**，只做静态障碍处理。依据：更像速度错误；不应假设人/车主动来撞，若此时锁角速度反而顶死。
3. **ROI 近距才计入**：仅当 `T_near` 内外推轮廓落入机器人轮廓**前后 + 左右**危险盒（ROI）并进入近距时，才可能有碰撞风险并计入威胁；远前方/远后方/远侧方不触发。
4. **任一障碍经迟滞判定为威胁** → 本周期建**一条**全局 `EdgeNearHorizonOmegaHold`；否则不建。

**边的语义**：

- `omega_ref` = 当前实测/下发角速度。
- 约束为 **变化范围**：`|omega_i - omega_ref| <= delta_omega_allow`（优先软约束），**不锁死** `omega = omega_ref`。
- 依据：完全锁死会导致近处不能转向、近似直线冲向障碍、规划失败；目标禁的是大幅度突变，不是禁转向。
- `delta_omega_allow` 宜与 `acc_lim_theta`、`T_near` 同量级协调；只作用于前 K 段 / 时间 `t <= T_near`，后段仍可转弯。

**配套**：重叠/威胁未命中时，动态物近场仍必须能走静态障碍边，避免两边都不建。

### 落地模块对应（实现时）

| 模块 | 作用 |
|------|------|
| 弱化/停用 `AddEdgesDynamicObstacles` | 避免不可靠速度驱动绕行 |
| 近场动态 → 静态障碍边 | 用当前位置保证近场几何间距 |
| 短时限幅外推 + ROI/重叠门控 | 决定是否启用 OmegaHold |
| `EdgeNearHorizonOmegaHold`（2c） | 近场短时 `\|omega - omega_ref\| <= delta_omega_allow`（软） |
| 动态障碍短时缓存 + 迟滞 | 抗断帧、防约束闪烁 |
| 调试话题 | enable 与否、触发障碍、ROI/距离 |

### 推荐实施顺序

1. 近场动态按静态位置建边 + 停用/旁路主路径上的 `AddEdgesDynamicObstacles`。
2. 实现门控（限幅外推、重叠排除、ROI）+ 2c 角速度变化范围约束。
3. 动态障碍短时缓存与进入/退出迟滞 + 调试输出。

---

## 编码方案（实现规格）

### 参数（开关不写死）

| 参数 | 默认 | 含义 |
|------|------|------|
| `dynamic_safety_predictable_mode` | true | 总开关：关旧动态时空边主路径，启用门控+OmegaHold |
| `dynamic_obstacles_as_static_edges` | true | 动态物是否按当前位置建静态障碍边（独立开关） |
| `dynamic_obstacle_cache_time` | 0.3 | 断帧缓存保持时间 [s] |
| `dyn_gate_T_near` | 1.0 | 门控/OmegaHold 近场时间窗 [s] |
| `dyn_gate_v_x_max` | 2.0 | 外推前障碍线速度夹紧 [m/s] |
| `dyn_gate_omega_z_max` | 1.0 | 外推前障碍角速度夹紧 [rad/s]（平面速度模长亦受 v_x_max 限制） |
| `dyn_gate_inflation` | 0.3 | 障碍轮廓膨胀 [m] |
| `dyn_gate_overlap_dist` | 0.05 | 与机器人当前轮廓过近则排除 OmegaHold [m] |
| `dyn_gate_roi_front/rear/left/right` | 1.0 | 相对 footprint 各边外扩触发带 [m]（取消统一 trigger_dist） |
| `dyn_gate_hold_time` | 0.3 | 威胁消失后仍保持 enable 的时间迟滞 [s]（不用 hold_frames / exit_dist） |
| `delta_omega_scale` | 0.3 | `|omega-omega_ref| <= scale * max_vel_theta` |
| `weight_near_horizon_omega_hold` | 50.0 | OmegaHold 软约束权重 |
| `publish_near_horizon_debug` | true | 发布 RViz Marker + 日志 |

### 文件与职责

| 文件 | 职责 |
|------|------|
| `near_horizon_dynamic_safety.h/.cpp` | Cache + ThreatGate + Marker 填充 |
| `g2o_types/edge_near_horizon_omega_hold.h` | `|omega-omega_ref|<=delta` 软边 |
| `optimal_planner.*` | 静态边 skip 开关；关/开动态边；`AddEdgesNearHorizonOmegaHold` |
| `teb_config.*` / `teb_params.yaml` | 参数 |
| `teb_local_planner_ros.*` | 更新 cache、评估门控、写 runtime、发 Marker/日志 |

### buildGraph 分流

```text
AddEdgesObstacles*:
  if isDynamic && include_dynamic && !dynamic_obstacles_as_static_edges -> skip
  else 按当前位置建静态边

if dynamic_safety_predictable_mode:
  不调用 AddEdgesDynamicObstacles（主路径）
  if runtime.omega_hold_enable: AddEdgesNearHorizonOmegaHold
else if include_dynamic_obstacles:
  AddEdgesDynamicObstacles  // 旧行为
```

### RViz 可视化（`teb_near_horizon_debug` MarkerArray）

- 机器人 footprint（当前）
- ROI 盒（footprint 四边外扩）
- 缓存动态障碍当前位置轮廓
- `T_near` 内外推采样轮廓（半透明）
- 触发障碍高亮；文本：enable / omega_ref / delta_allow / 触发 id

### 日志

`RCLCPP_INFO_THROTTLE`：cache 数量、raw_threat、enable、重叠排除、触发 id、omega_ref、delta_allow；模式切换用 INFO。

---

## 轨迹可行性 / 轮廓碰撞检查（已拍板原则）

> 地位：**放行闸门**（硬否决），不是优化建边手段。与近场 OmegaHold「弱化速度预测驱动绕行」不矛盾：预测只用于短时否决，不用于拉动大幅度机动。  
> **实现状态**：已接入 `TebOptimalPlanner::isTrajectoryFeasible` → `isTrajectoryContourFeasible`（参数见 `trajectory.feasibility_check_*` / `feas_pred_*`）。

### 为何需要（现状缺口）

当前 `isTrajectoryFeasible` **只查局部代价图 footprint**（`ObstacleFootprintCritic::scorePose`）。  
优化边使用的 `obstacles_`（costmap 转换多边形、`obstacles` 话题自定义障碍等）**不参与**该硬检查。  
结果：非代价图数据源的轮廓可能导致「优化看得见、可行性放行」的误判。

### 原则总览

```text
放行 = 代价图 footprint（现有）
     ∧ 全路径：静态轮廓 + 动态物【当前位置】轮廓
     ∧（可选附加）短时短距动态【预测】轮廓 —— 仅当角速度门控 OR 命中时才执行
```

失败处理与现有一致：不可行 → 清零速度 / `clearPlanner` / 计入 infeasible（与当前可行性失败路径对齐）。

### A. 全路径当前轮廓检查（建议必做）

| 项 | 约定 |
|----|------|
| 范围 | **整条**待检局部路径（或与现有 `feasibility_check_no_poses` / `feasibility_check_lookahead_distance` 策略明确对齐后的「规划全路径」语义；意图是覆盖整段输出轨迹，而非仅近场几帧） |
| 静态障碍 | 容器内静态障碍的**当前轮廓** vs 机器人 footprint |
| 动态障碍 | 仅用**当前位置轮廓**（不依赖速度） |
| 判据 | 几何相交 / 内部占用，或距离 ≤ 很小裕度；避免只信无符号「到边界距离」导致大多边形内部漏检 |
| 与速度关系 | **不依赖**动态速度；补齐代价图外数据源的几何底线 |

### B. 动态预测轮廓检查（附加否决，可开关）

**不做**全程、长时预测碰撞作为日常拦网；预测检查必须同时满足：

1. **时间窗** `T_pred`：短时（建议与 `dyn_gate_T_near` 同量级，如 0.5–1.5 s）。  
2. **距离窗** `R_check`：仅近距内动态物参与。  
3. **角速度门控（OR）**：见下一小节；**任一**条件满足才做预测碰撞，否则跳过预测检查（仍保留 A + 代价图）。

外推前仍应对障碍速度做夹紧（如 `dyn_gate_v_x_max`），避免离谱速度画出巨大禁区。  
时空对齐优先用 TEB 轨迹时间（累加 `TimeDiff`）与障碍恒速外推时刻对应。

#### 角速度门控（已拍板：OR）

在检查窗（时间/距离）内，对每段：

\[
\omega_i = \frac{\mathrm{normalize}(\theta_{i+1}-\theta_i)}{\mathrm{TimeDiff}(i)}
\]

与 `EdgeVelocity` / `EdgeNearHorizonOmegaHold` **同一公式**。  
不要另造 Δt（固定控制周期、弧长/线速度反推、墙钟等）。`dt` 过小需保护（与 OmegaHold 一致）。

门控打开预测检查，当且仅当：

```text
(max_i |ω_i| > δ_abs)  ∨  (max_i |ω_i − ω_ref| > δ_rel)
```

| 条件 | 含义 | 多拦场景 |
|------|------|----------|
| `max\|ω\| > δ_abs` | 轨迹绝对转率大 | 持续大弯、掉头等（即使 ω≈ω_ref） |
| `max\|ω−ω_ref\| > δ_rel` | 相对当前执行角速度突变 | 急甩头 / 急回正 |

- **必须用或（OR）**：满足一个即可打开预测检查 → 覆盖面更大、更严。  
- **不要用与（AND）**：AND 比单独任一更松，并不更安全。  
- `ω_ref`：与 OmegaHold 相同，取当前实测/下发角速度（如 `robot_vel.angular.z`）。  
- `δ_rel`：可与 OmegaHold 同源（如 `k * delta_omega_scale * max_vel_theta`，`k≥1` 表示软约束未按住才上硬检查），或独立参数但语义仍是「相对参考的变化量」。  
- `δ_abs`：宜略松（接近 `max_vel_theta` 量级或略低），减轻正常弯道被预测误杀。  
- 建议模式枚举：`rel` / `abs` / `both_or`（默认 `both_or`）；日志区分由 `abs` 还是 `rel` 触发。

#### 预测检查与近场可预测目标的关系

| 层级 | 速度预测角色 |
|------|----------------|
| 优化 / OmegaHold | 弱化：不以长时预测驱动大幅度绕行 |
| 放行闸门 A | 不依赖速度：当前轮廓硬底线 |
| 放行闸门 B | 短时预测仅作**否决**；且仅在路径已出现大 \|ω\| 或大 \|ω−ω_ref\| 时启用 |

含义：计划本身已不够「可预测」（大转率或相对当前突变）时，再叠加短时动态预测碰撞否决；计划近似延续当前角速度时，不拿不可靠预测去否决放行。

### 已知取舍（文档须明示）

- **低 ω 且低 \|ω−ω_ref\| 的迎面/斜向接近**：B 不触发；若此刻轮廓尚未压到路径上，可能仍放行。这是「可预测优先、少用预测否决」的刻意缺口，不是算 ω 方式能补上的。若日后不可接受，可另加不依赖 ω 的窄条件（如近距 ∧ ‖v‖>v_min ∧ 短时 TTC/预测相交），与本节 OR 门控并列，需另行拍板。  
- OR 打开 `max\|ω\|` 后，弯道场景预测误杀（假阳性）会增多 → 靠偏松 `δ_abs` + 短 `T_pred`/`R_check` 压副作用。  
- 薄线 / 大多边形：硬检查用相交或有符号内部判定，与优化软边距离语义区分开。

### 建议检查顺序（实现时）

```text
1. 代价图 footprint（现有 isTrajectoryFeasible）
2. 全路径：obstacles_ 静态轮廓 + 动态当前位置轮廓
3. 若 (max|ω|>δ_abs) ∨ (max|ω−ω_ref|>δ_rel)：
     在 T_pred ∩ R_check 内做动态预测轮廓时空碰撞
4. 全部通过 → 放行 cmd_vel
```

### 编码参数（已实现）

| 参数 | 含义 |
|------|------|
| `feasibility_check_obstacle_contours` | 是否启用全路径当前轮廓检查 |
| `feasibility_check_dynamic_prediction` | 是否启用预测轮廓附加检查 |
| `feas_contour_margin` | 轮廓碰撞裕度 [m] |
| `feas_pred_T` / `feas_pred_R` | 预测检查时间窗 [s] / 距离窗 [m] |
| `feas_pred_gate_mode` | `rel` / `abs` / `both_or` |
| `feas_pred_delta_omega_abs` | `δ_abs` [rad/s] |
| `feas_pred_delta_omega_rel` | `δ_rel` [rad/s]；`<=0` 则用 `delta_omega_scale*max_vel_theta` |
| `feas_pred_v_min` | 参与预测检查的障碍最小速度模长 |

---

## 讨论日志


### 2026-08-01 11:57 (UTC+8)

**议题**：在现有 TEB 框架下落地核心目标的技术方案；并确认若干分歧点。

**讨论要点与结论**：

1. **`AddEdgesDynamicObstacles` 是否必要**  
   - 只靠近场“禁止大幅度转向”能落实可预测性，但不能单独保证与动态物的近场几何安全。  
   - 决定：不以动态时空边为主；近场动态障碍改为按静态位置建边（已同意）。

2. **是否限线速度；2a/2b/2c 取舍**  
   - 决定：暂不限线速度，只约束角速度。  
   - 不做 2a/2b 硬降 `max_vel_theta`。  
   - 采用 2c，且改为“保持当前角速度附近”，而非优化至 0。

3. **动态数据抖动 / 断帧**  
   - 决定：做短时缓存（约 0.2–0.5 s）+ 进入/退出迟滞，保证近场门控与角速度保持稳定。

4. **与核心目标的一致性**  
   - 速度不可靠 → 不依赖长时动态预测绕行。  
   - 安全 = 近场短时轨迹可预测 = 近场不出现相对当前状态的大幅度转向变化。  
   - 位置层近场静态建边补齐几何底线，不升格为并列目标条文。

### 2026-08-01 15:20 (UTC+8)

**议题**：`EdgeNearHorizonOmegaHold` 的激活条件、速度外推用法、以及是否锁死角速度。

**讨论要点与结论**：

1. **不能对所有运动障碍都建 OmegaHold**  
   - 否则无关运动体也会锁转向，可能导致前方静态绕障规划失败。  
   - 决定：仅当 `T_near` 内双方轮廓有近距碰撞可能时才启用。

2. **速度用于短时外推轮廓，但必须限幅**  
   - 门控以位置 + 膨胀为主；速度不是弱提示，而是用于外推障碍轮廓。  
   - 外推前对障碍速度夹紧：最大线速度 `v_x_max`、最大角速度 `omega_z_max`；只在 `T_near` 内推进，并保留膨胀防漏触发。  
   - 可用稍大触发距离换保守，但必须配合迟滞防抖。

3. **外推/当前轮廓已与机器人当前轮廓重叠 → 不建 OmegaHold**  
   - 只做静态障碍处理。  
   - 依据：很可能是速度信息有问题；不应假设运动人/车主动来撞；此时锁角速度更容易顶死。

4. **OmegaHold 不锁死 `omega_ref`，只限制变化范围 `delta_omega_allow`**  
   - 优先软约束；只作用近场前 K 段。  
   - 依据：完全锁死当前运动状态会导致近处完全不能转向、沿当前角速度（含直行）冲向障碍、规划失败；目标是限制大幅度转向变化，不是禁止转向。

5. **ROI 裁剪**  
   - 计入威胁的外推轮廓应主要落在机器人前后 + 左右距离范围内；远前方/远后方/远侧方无碰撞风险，不触发。

6. **采纳的工程附加项**  
   - 进入/退出迟滞；只约束 `t <= T_near` 前段；全局一条 OmegaHold（任一威胁命中则 enable）；调试输出 enable/触发障碍/门控距离。

7. **门控流程（定稿摘要）**  
   - 限幅速度 → 短时外推轮廓 → 已重叠则排除（仅静态）→ 否则看是否进入 ROI 近距 → 迟滞后 enable 一条 `|omega - omega_ref| <= delta_omega_allow` 边。

### 2026-08-01 15:54 (UTC+8)

**议题**：编码参数修订 + RViz 可视化；开始按编码方案实现。

**结论**：

1. `dynamic_obstacles_as_static_edges` 独立开关，不写死。  
2. ROI 用 footprint 四边外扩，取消 `dyn_gate_trigger_dist`。  
3. 用 `dyn_gate_hold_time`，取消 `hold_frames` / `exit_dist`。  
4. `delta_omega_scale * max_vel_theta` 作为允许角速度偏离。  
5. 增加 `teb_near_horizon_debug` MarkerArray（footprint / ROI / 外推轮廓 / 触发高亮）。  
6. 编码方案写入本文「编码方案（实现规格）」并开始落地代码。

### 2026-08-04 (UTC+8)

**议题**：规划路径安全检查是否纳入障碍轮廓碰撞，以及动态预测轮廓检查的门控方式。

**背景问题**：

- `isTrajectoryFeasible` 仅查局部代价图，`obstacles_` 中非代价图来源轮廓可能导致误判放行。  
- 动态速度不可靠：不宜用长时预测驱动绕行，但放行闸门上可用短时预测作保守否决。

**讨论要点与结论**：

1. **全路径当前轮廓（必做原则）**  
   - 静态障碍轮廓 + 动态障碍**当前位置**轮廓，在规划全路径范围做碰撞检查。  
   - 不依赖速度；补齐代价图外数据源的硬几何底线。

2. **动态预测轮廓（附加否决）**  
   - 限制在设定时间窗 `T_pred` 与距离窗 `R_check` 内。  
   - **仅当角速度门控命中时才检查**；否则不做预测碰撞（仍保留当前轮廓 + 代价图）。

3. **ω 怎么算**  
   - 用 TEB `TimeDiff`：`ω = normalize(Δθ) / TimeDiff`，与 Velocity / OmegaHold 边一致。  
   - 「相邻 pose 的 Δθ/Δt」在 Δt=`TimeDiff` 时与上式相同；禁止另造 Δt。

4. **门控指标：`max|ω|` 与 `max|ω−ω_ref|` 同时开，用或（OR）**  
   - `(max|ω| > δ_abs) ∨ (max|ω−ω_ref| > δ_rel)`，满足**一个**即可打开预测检查。  
   - OR 更严、更安全；AND 更松，不用。  
   - `ω_ref` = 当前执行角速度；两套阈值独立，`δ_abs` 宜略松以免弯道误杀。

5. **与核心目标一致的定位**  
   - 轮廓/预测检查 = **放行硬闸门**，不是优化主手段。  
   - 预测否决只在路径已出现大绝对转率或相对当前突变时启用，避免日常用不可靠速度预测否决「可预测」轨迹。

6. **已知缺口（接受并写入原则）**  
   - 低 `|ω|` 且低 `|ω−ω_ref|` 时迎面接近：预测检查不触发；若需补强须另行拍板（如 TTC 窄条件）。

7. **文档**  
   - 以上结果写入本文「轨迹可行性 / 轮廓碰撞检查（已拍板原则）」；已在 `isTrajectoryContourFeasible` 落地。
