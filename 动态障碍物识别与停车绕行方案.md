# 动态障碍物识别与停车绕行方案

规划侧行为约束见同目录《动态障碍物避让.md》：动态近场不靠不可靠速度做长时绕行，宁可减速/短停，保持轨迹可预测。本文是感知与决策层：独立检测包如何把障碍分成静态/动态，以及前方路径上「动态停车、静态绕行」如何落地。

实现代码在仓库 `src/dynamic_obstacle_filter`，数据处理链路与算法细节见该包内《数据处理流程.md》。

## 核心目标

前方路径上出现障碍时：

- **确认在动**（行人、车辆、被推着走的物体）→ **停车**，保持朝向，等待其离开或转为静止。
- **确认静止**（墙、柱、放下后不再动的箱子、站住的人）→ **绕行**（重规划 / 局部静态避障）。
- **未决**（刚出现、轨迹太短、速度不可信）→ **对外按静态处理：允许绕行，不进动态话题**。

成功标准首先是不把静物卡成永久停车；横穿行人应在短窗口（`T_dyn`）内升为动态并停车。未决当静态会让行人入视野的前 0.3–0.5 s 被当静物绕，必须靠「进动态快 + ID 不丢」压住这个窗口。

假阳性（静当动）会让任务停死，对本策略比漏检更伤。漏检时只要局部 costmap 仍有占据，至少还能绕。

## 为什么不能直接用 Dynablox

Dynablox（[arXiv:2304.10049](https://arxiv.org/pdf/2304.10049v2)）的「动态」定义是：**当前点落入此前的高置信自由空间**。本方案要的「动态」是：**这个物体相对地图现在还在动**。两者不对齐。

| 前方情况 | 本方案应做 | 原版 Dynablox | 后果 |
|---|---|---|---|
| 行人从空地走进路径 | 停车 | 判动态 | 对 |
| 纸箱放到路上后不动 | 绕行 | 会先一直判动态 | 停在箱子前 |
| 行人走进去后站住 | 可绕或再等一小会 | 要等漂移复位 \(\tau_r\) 才可能变静 | 停太久或突然改绕 |
| 人一直站在路上、从未走进空区 | 绕行 | 当静态 | 碰巧对 |
| 人贴墙走、贴着未知边界走 | 停车 | 经常漏、残缺 | 当成静态去绕 |
| 回环跳变把墙标成动态 | 应继续走或绕墙 | 大片假动态 | 走廊里停死 |

结构问题：

1. 新放入自由空间且随后静止的物体，在几何上与正在走的人无法区分。
2. 动态簇体素 `w=0`，TSDF 不把它们融进静图，物体很难「转正」成可绕静障。
3. \(\tau_r\) 按里程计漂移估计，不是「物体停稳」时间。
4. 无跨帧跟踪，没有稳定 ID。
5. 假阳性（静当动）比漏检更伤。

因此：**只借用「静图之外的点是候选」这一线索，最终分类必须用世界系跟踪 + 时间迟滞。** 第一版不上完整 Voxblox/Dynablox，也不上无图自由空间补丁。

## 定义

内部三态，对外两态：

| 内部状态 | 含义 | 对外 | `~/dynamic_obstacles` | 挡路时 |
|---|---|---|---|---|
| 动态 | 相对 `map` 仍在运动 | 动态 | 发布 | 停车 |
| 静态 | 相对 `map` 当前不在运动（含图内结构、已停稳的新增物） | 静态 | 不发布 | 绕行 |
| 未决 | 轨迹太短、速度不可信 | **静态** | 不发布 | 绕行 |

未决仍进跟踪器，只是不进动态话题。否则没有跨帧证据，无法升为动态。

不要单独当作最终判决的信号：

- 语义类别（人/车）。站住的人按本策略应绕，不是永远停。
- 「点是否进过自由空间」。新放的箱子会一直像动态。
- 局部 costmap 是否占据。那只说明「这儿有东西」。
- 单帧聚类或「速度非零」。点云质心抖动会制造假速度。

TEB 约定：`costmap_converter_msgs/ObstacleMsg` 一旦写入速度且模长 ≥ 0.001，障碍会被标成 `isDynamic()`。本包动态话题的语义是 **「出现在该话题 = 确认在动、挡路则停」**，不靠速度是否非零区分未决。未决不进该话题。

## 独立检测包

独立 ROS2 节点（例如 `dynamic_obstacle_detector`）。只回答：地图外这些簇现在是未决、在动、还是已停稳。

不负责：路径膨胀带求交、`cmd_vel=0`、改 TEB 权重、无图 ever-free 补丁。  
静图墙/柱不进跟踪器，也不进 `ObstacleArrayMsg`。

不要把 `~/dynamic_obstacles` remap 到 TEB 的 `obstacles`：未决已改为对外静态；若误接到 TEB，填了速度的动态物会走近场 OmegaHold，而不是「停车等离开」。TEB 对该话题做**独立订阅** + PATH 走廊门控（见「停车 / 绕行门控」）。

## 总体流程

```text
PointCloud2（已去地，或本节点先调 ground_segmentation_cuda）
  → tf 到 map（用云的 stamp，禁止 TimePointZero 估速）
  → 点级：到 Cartographer 占据格距离 ≶ map_assoc_dist
       ├─ 贴图点：丢弃（固有静态，不跟踪）
       └─ 残差点：机器人周围 ROI 裁剪（8–15 m）
            → 欧氏聚类（N_min、最小投影面积）
            → 簇级复核（太像墙皮则丢）
            → Kalman 预测 + 多线索关联 + 一对一匹配
            → 迟滞状态机：未决 / 动态 / 静态
            → 发布动态话题 + 可视化
```

分类不绑导航。路径求交在 TEB 门控：用 `~/dynamic_obstacles` 与前方 PATH 走廊相交。

不要在感知里把动态点从静图里永久抹掉。停车只看跟踪状态；绕行只把 **已确认静止** 的簇写入 local costmap 静态层。未决凸包还在变，第一版不要写入静态层。

## 候选提取

「地图内固有占据」和「地图外异常占据」是同一次点级距离测试的两端，不是先聚类再整簇贴标签。

```text
非地面点
  → 点级：到静图距离 ≶ map_assoc_dist
       ├─ 贴图点：丢弃
       └─ 残差点：聚类 → 过小/像墙皮则忽略 → 其余进跟踪器
```

**不要**对整朵非地面点云先欧氏聚类再判整簇在不在图里。人贴墙会和墙粘成一簇，墙点占多数时整簇被判固有静态，人被吃掉。

静图（Cartographer `OccupancyGrid`）建议先做占据距离变换，点查询 O(1)。2D 栅格用 \(xy\) 到占据格的距离。`map_assoc_dist` 偏紧（0.15–0.25 m）：太大，贴墙行人被扣光；太小，墙皮噪声进跟踪器。

无图区：关联不上的点全部落成残差，进跟踪器，靠状态机把墙判静。代价是驶入未建图区域可能先按未决（对外静态）绕墙。自由空间种子补丁放到 v2。

过小簇（点数 < 约 20 或投影面积过小）：不建轨，直接忽略。不要给噪声一个未决 ID，否则路上会凭空出现「可绕静障」。

## 世界系跟踪

速度必须在 `map` 下计算。机器人向前开时，传感器系里整片点云都在往后飞，会把墙当成迎面而来。

Kalman 状态：平面常速 \((x,y,v_x,v_y)\)。观测：簇质心。\(\Delta t\) 用两帧 stamp 差，不假设固定帧率。不要用两帧原始质心差分当速度。

ID：单调递增 `int64`，删除后冷却数秒再复用。

### 关联：多线索 + 预测硬门 + 匈牙利

质心随凸包视角会跳；纯 IoU 在贴墙残缺、腿/身分裂时经常掉到 0。单用哪个都不稳。

对上一帧轨迹 \(i\)（Kalman 预测到本帧时刻）和本帧簇 \(j\)：

**硬门（任一不满足则禁止匹配）**

1. 预测质心距离 \(\|\hat{c}_i - c_j\| < d_{\max}\)。室内 \(d_{\max} = 1.2\)–\(1.5\,\mathrm{m}\)；\(\Delta t\) 变大时 \(d_{\max} \leftarrow d_{\max} + v_{\mathrm{cap}}\Delta t\)，\(v_{\mathrm{cap}}\approx 2\,\mathrm{m/s}\)。
2. 面积比 \(A_j / A_i \in [0.3, 3]\)（或 \(\sqrt{A}\) 比）。
3. 点数比 \(n_j / n_i \in [0.25, 4]\)。

IoU 为 0 **不**作为硬门否决。

**代价（越小越好，匈牙利一对一）**

\[
\begin{aligned}
\mathrm{cost}(i,j)
&= w_d \cdot \frac{\|\hat{c}_i - c_j\|}{d_{\max}}
+ w_{\mathrm{iou}} \cdot (1 - \mathrm{IoU}_{xy}) \\
&+ w_{\mathrm{pt}} \cdot (1 - r_{\mathrm{pt}})
+ w_a \cdot \frac{|\sqrt{A_i}-\sqrt{A_j}|}{\max(\sqrt{A_i},\sqrt{A_j})}
\end{aligned}
\]

| 线索 | 建议权重 | 作用 |
|---|---|---|
| 预测质心距离 | \(w_d = 1.0\) | 主线索；必须用预测位置，不用上一帧原始质心 |
| 鸟瞰凸包 IoU | \(w_{\mathrm{iou}} = 0.8\) | 同一物体两帧重叠时稳 |
| 点落入扩张旧凸包比例 \(r_{\mathrm{pt}}\) | \(w_{\mathrm{pt}} = 1.0\) | 比 IoU 更抗形状跳变；旧凸包外扩 0.15–0.20 m |
| 面积（等效半径） | \(w_a = 0.3\) | 防噪声簇对上 |

**匹配流程**

1. 所有轨迹预测到本帧 stamp。
2. 算代价矩阵，硬门外标 \(\infty\)。
3. 匈牙利一对一（簇少时贪心也可）。
4. 匹配成功：沿用 ID，用本帧质心/凸包更新 Kalman。
5. 未匹配簇：新 ID，内部 = 未决。
6. 未匹配轨迹：**coast 2–4 帧**（常速滑行，不改对外状态）。仍无匹配再删。Coast 期间动态仍发布、静态仍不发布。禁止一丢检就新开未决（否则对外又变成可绕）。

**分裂 / 合并**

- 一轨对上两簇：留给代价更小的，另一簇新生。
- 两轨对上同一簇：留给更老的 ID，或已是动态的那条；另一条删除。

不要用：原始质心差分当速度、3D 框 IoU、语义类别、把贴图点算进凸包。

## 动静判定

滤波后：

\[
v=\sqrt{v_x^2+v_y^2},\quad
d_{\mathrm{win}}=\|c(t)-c(t-T_{\mathrm{win}})\|,\quad
T_{\mathrm{win}}\approx 0.5\,\mathrm{s}
\]

\(I_{\mathrm{win}}\)：与 \(T_{\mathrm{win}}\) 前凸包的鸟瞰 IoU。质心抖但人没走时，\(v\) 可能虚高，但 \(d_{\mathrm{win}}\) 小、\(I_{\mathrm{win}}\) 大。

**判「在动」：同时满足，并连续持续 \(T_{\mathrm{dyn}}\)**

1. \(v > v_{\mathrm{dyn}}\)
2. \(d_{\mathrm{win}} \ge d_{\mathrm{win,min}}\)
3. 窗位移方向与速度夹角 \(< 60^\circ\)（去掉质心来回抖）
4. 弱条件二选一：窗 IoU < 0.5 **或** 点云相对旧框在外扩

缺任一条：不能切入动态。

遮挡揭开否决（仅未决/确认静止 → 动态）：后方静物露出时可见轮廓往一侧长，质心会动，上面四条都能过。若沿窗位移方向投影后，长度变化比例 > `occ_len_ratio`（0.35）且有一端位移 < `occ_anchor_dist`（0.12 m），则本帧不算在动。已确认动态的目标不做此否决。

**判「停稳」：同时满足，并连续持续 T_settle**

1. \(v < v_{\mathrm{stat}}\)
2. \(d_{\mathrm{win}} < d_{\mathrm{win,min}}\)
3. \(I_{\mathrm{win}} > 0.5\)

中间带 \(v_{\mathrm{stat}} < v < v_{\mathrm{dyn}}\)：保持上一内部状态。

原则：**进动态要快，进确认静态要慢。** 未决对外已经是静态，「进确认静态慢」只影响 Marker 黄→绿，不改变绕行是否发生。

### 建议初值（室内覆盖车，实车再标定）

| 参数 | 建议值 | 含义 |
|---|---|---|
| `map_assoc_dist` | 0.15–0.25 m | 点级贴图，视为固有静态 |
| `d_max` | 1.2–1.5 m | 关联质心硬门 |
| `v_cap` | 2.0 m/s | 关联门限随 \(\Delta t\) 放宽 |
| `hull_expand` | 0.15–0.20 m | 点落入旧凸包时的外扩 |
| `v_dyn` | 0.30 m/s | 高于此，倾向动态 |
| `v_stat` | 0.12–0.15 m/s | 低于此，倾向静态 |
| `T_dyn` | 0.3–0.5 s | 连续满足后才切入动态（宜短） |
| `T_settle` | 1.5–3.0 s | 连续满足后才切入确认静态 |
| `T_win` | 0.5 s | 位移 / 窗 IoU 窗口 |
| `d_win_min` | 0.10–0.15 m | 窗内净位移下限（判动态） |
| `N_min` | 3 帧 | 未决至少观察这么久 |
| `coast_frames` | 2–4 帧 | 丢检保持原对外状态 |
| `occ_len_ratio` | 0.35 | 沿位移轴长度变化，遮挡揭开否决 |
| `occ_anchor_dist` | 0.12 m | 生长轴一端几乎不动则否决进动态 |
| 中间带 | `v_stat < v < v_dyn` | 保持上一内部状态 |
| ROI | 机器人周围 8–15 m | 第一版不按路径裁 |
| 路径膨胀 | 足迹半宽 + 安全距（须大于人体厚度余量） | 门控与前方路径求交 |

### 状态机

```text
新簇 / 新 ID
  → 未决（对外静态，不进 dynamic_obstacles，挡路可绕）

未决
  ├─ 连续 T_dyn 满足「在动」且非遮挡生长 → 动态
  ├─ 连续 T_settle 满足「停稳」 → 静态（确认）
  └─ 否则保持未决（对外仍静态）

动态
  ├─ 连续 T_settle 满足「停稳」 → 静态
  ├─ coast 中 → 保持动态（继续发布）
  └─ 否则保持动态（挡路停车）

静态（确认）
  ├─ 连续 T_dyn 满足「在动」 → 动态
  └─ 否则保持静态（挡路绕行）
```

定位跳变（平移突跳数厘米或协方差暴涨）：冻结分类 0.5–1 s，已有轨迹维持原对外状态；冻结期内禁止未决→动态。

## 产品话题：ObstacleArrayMsg

消息：`costmap_converter_msgs/ObstacleArrayMsg`（元素 `ObstacleMsg`）。字段只有 `id`、`polygon`、`radius`、`orientation`、`velocities`，没有动静枚举，靠话题约定。

| 话题 | 类型 | 内容 |
|---|---|---|
| `~/dynamic_obstacles` | `ObstacleArrayMsg` | **仅内部状态 = 动态** |

确认静止、未决、贴图点：都不进该话题。交给门控时：**出现在这个话题里 = 挡路则停**。

每帧都发，允许 `obstacles` 为空（表示当前没有需要停车的对象，避免 latch 幽灵）。`header.stamp` 用当前点云时间，`header.frame_id` 固定 `map`。频率跟点云或限到 10 Hz。QoS：reliable、depth 1。

**每个 `ObstacleMsg`**

- `id`：跟踪 ID，跨帧稳定。
- `polygon`：残差簇 xy 凸包，`z=0`，顶点 ≥ 3。点太少时退化成 1 点 + `radius`（下限约 0.25 m）。
- `radius`：多边形时填 0；圆形退化时填半径。
- `orientation`：速度方向 yaw；速度太小则保持上一帧或 0。
- `velocities.twist.linear.x/y`：Kalman 在 **map 系** 的平面速度。角速度与 `linear.z` 填 0。速度只用于分类/可视化，不用于 TEB `AddEdgesDynamicObstacles` 长时预测绕行。

## 可视化话题

Marker 默认开，过程点云可关。

**`~/markers`（`visualization_msgs/MarkerArray`）**

每帧整包发布，`action=ADD`，`lifetime` 略大于周期（例如 0.3 s）。

| ns | 含义 | 画法 |
|---|---|---|
| `hull_pending` | 未决（对外静态） | 凸包 LINE_STRIP，黄 |
| `hull_dynamic` | 确认在动（已进话题） | 凸包 LINE_STRIP，红 |
| `hull_static` | 已停稳（不进话题） | 凸包 LINE_STRIP，绿 |
| `id` | 跟踪 ID 与状态字母 | TEXT_VIEW_FACING |
| `vel` | 速度 | ARROW，长度 ∝ \(\|v\|\) |
| `centroid` | 质心 | SPHERE，颜色同状态 |

静态/未决虽不进产品话题，Marker 必须画，否则无法标定 `T_dyn` / `T_settle`。

**过程点云（`sensor_msgs/PointCloud2`）**

| 话题 | 内容 |
|---|---|
| `~/cloud_map_associated` | 被静图扣掉的点 |
| `~/cloud_residual` | 残差点（聚类输入） |
| `~/cloud_dynamic` | 当前动态簇的点 |
| `~/cloud_static_tracked` | 已停稳簇的点 |

第一版至少：`~/markers` + `~/cloud_residual`。

可选：`~/map_assoc_grid` 发布局部距离变换/贴图掩膜，检查雷达与 Cartographer 是否对齐。

## 停车 / 绕行门控（已落地：TEB 内，不进 `obstacles`）

分类只说明物体属性，还要看它在不在前方 **PATH** 上。门控做在 `TebLocalPlannerROS::computeVelocityCommands`，`planner_->plan()` 之前。

**不要**把 `/dynamic_obstacles` remap 到 TEB 的 `obstacles`。该话题一旦写入速度会 `isDynamic()`，走进场 ROI + OmegaHold，看的是车头近场并开始绕，而不是「沿路径停车等待」。costmap 静障仍走原 TEB；动态停车只看独立订阅。

本仓库 FollowPath 即 TEB，无独立 BT 可挂。停车时 **成功返回减速/零速**，禁止抛 `PlannerException`（否则会进原地转/后退 recovery）。

```text
/dynamic_obstacles          costmap / 静图
        │  仅确认动态              │  墙、箱子、站住的人、未决
        ▼                         ▼
TEB 独立订阅              obstacles_（现有，当静态）
        │
        ▼
transformGlobalPlan 之后、原地转 / plan() 之前
        │
        ├─ PathWaitGate：PATH 走廊 ∩ 动态多边形
        │     WAIT    → 不 plan，按 acc_lim 收到 0，ω→0，保持朝向
        │     DETOUR  → 正常 TEB（costmap 静态绕）；该 ID latch 直到走廊清空
        │     FOLLOW  → 正常跟路径
        │
        └─ 近场 OmegaHold（现有，机器人 ROI；不订本话题）
```

### 路径走廊

问的是：若继续贴 PATH 走，会不会已经碰到 TEB 对静障也不肯贴的距离。

```text
r_foot = max( costmap 足迹顶点 |y| , footprint_model 内切半径 )
         circular 时即为 radius

half_width = r_foot + min_obstacle_dist * path_wait_corridor_scale

scale 默认 1.0。贴边模式若临时改小 min_obstacle_dist，走廊仍用初始化时的
正常 min_obstacle_dist，避免半宽被收到 0.05 m。
```

用 `min_obstacle_dist`，不用 `inflation_dist`。不要再叠第三层「人体厚度」；凸包偏瘦用 `scale>1`。

纵向：从机器人在 `global_plan_` 上的最近点起，沿路径弧长 `path_wait_lookahead_dist`（默认 6 m），与 `max_global_plan_lookahead_dist`（局部优化窗 3 m）解耦。胶囊两端用同一个 `half_width` 做圆帽。求交**不用**障碍速度。

### 状态机（无规划侧 T_settle）

停稳由检测包 `t_settle` 完成后物体**离开** `/dynamic_obstacles` 表达。规划侧不再抄一套停稳时钟。需要的是：

| 参数 | 默认 | 含义 |
|---|---|---|
| 检测 `t_settle` | 2.0 s | 连续安静后出话题、改当静态 |
| `path_wait_timeout`（T_wait） | 6.0 s | **每个未锁 ID** 第一次挡路起，最多停多久后只 latch 该 ID |
| `path_wait_clear_time`（T_clear） | 0.5 s | 走廊空了才清全部 latch；单 ID 离开 T_clear 后丢掉该 ID 的钟（去抖） |
| `path_wait_msg_timeout` | 0.5 s | 话题断流不当动态 |

约束：`T_wait > t_settle`，这样站住的人走出话题再绕，而不是被超时 latch 锁成 DETOUR。

`T_wait` 按 ID 独立，不共用一把全局钟：新人有完整等待；老人已等时间保留；闪出不到 `T_clear` 不重新计时。

```text
FOLLOW
  存在未锁 ID 挡路 → WAIT（该 ID 记下 t_enter=now）
  仅有已锁 ID 挡路 → DETOUR

WAIT
  blocking 空并保持 T_clear → 清全部钟与 latch → FOLLOW
  任一未锁 ID：now - t_enter < T_wait → 继续停
  某未锁 ID：now - t_enter ≥ T_wait → 只 latch 这个 ID
  仍有未锁 ID 未满 T_wait（含离开不足 T_clear）→ 继续 WAIT
  未锁都已超时或走光 → DETOUR

DETOUR
  正常 plan（costmap 静障绕行）
  走廊清空 T_clear → 清全部钟与 latch → FOLLOW
  某已锁 ID 离开走廊满 T_clear → 解开该 ID（再进来算新遭遇）
  新出现、未锁的 ID 挡路 → WAIT，只给这个 ID 开钟
  已锁 ID 仍挡路 → 保持 DETOUR
```

| 前方情况 | `/dynamic_obstacles` | 门控 | 行为 |
|---|---|---|---|
| 墙/柱/已在静图里 | 空 | FOLLOW | 现有 TEB 绕 |
| 箱子刚放下、未决 | 空 | FOLLOW | 当静态绕，不停死 |
| 行人横穿且仍在动 | 有多边形 | WAIT | 停，保持朝向 |
| 行人在超时前离开走廊 | 空或不再相交 | FOLLOW | 接着走原 PATH |
| 行人站住（检测 `t_settle` 后出话题） | 空 | FOLLOW | costmap 仍占据 → 绕 |
| 等满 T_wait 人还在动 | 仍有 | DETOUR latch | 当静物绕开；该 ID 仍在走廊里不再停 |
| WAIT 中又来一个新人 | 新 id | WAIT | 新人单独开钟，老人钟不停；未锁都超时才绕 |
| 绕行途中又来未锁 ID | 新 id 或离开过 T_clear 的旧 id | WAIT | 单独再等 |

WAIT 输出：`acc_lim_x` / `acc_lim_theta` 逼近 0；发仅含当前位姿的 `local_plan`；FollowPath 保持成功。DETOUR / FOLLOW 维持现有优化与可行性检查。

调试话题：`teb_path_wait_debug`（走廊、挡路凸包、状态字、id、已等时间）。

实现：`path_dynamic_wait.h/.cpp`；参数见 `teb_params.yaml` 的 `path_wait_*`。

## 异常与冻结

| 情况 | 处理 |
|---|---|
| 机器人自身运动补偿不全 | 先查贴图墙若被误跟踪，速度是否≈0；否则先修 TF / 时间同步 |
| 人贴墙走 | 先点级扣静图再聚类；关联以点落入旧框为主，IoU 为辅 |
| 人站住 | `T_settle` 后变确认静态并绕开 |
| 箱子刚放下 | 未决对外已是静态，可绕；禁止用自由空间占用把箱子锁死在动态 |
| 行人刚入视野 | 前 `T_dyn` 未决=可绕；必须短 `T_dyn` + 稳关联，尽快变红停车 |
| 后方静物被前方遮挡后露出 | 一端钉住且沿位移轴变长 → 否决进动态，保持未决/静态 |
| 回环 / 位姿跳变 | 冻结 0.5–1 s，维持原对外状态，禁止未决→动态 |
| 远距稀疏散点 | 不建轨 |
| 跟踪丢失 | coast 2–4 帧后删除；动态在 coast 期间仍发布 |

## 与现有栈的对应

| 模块 | 角色 |
|---|---|
| `ground_segmentation_cuda` | 去掉地面，得到非地面点 |
| Cartographer 静图 | 点级固有静态；残差产生候选 |
| 本方案跟踪 / 状态机 | 输出 ID、多边形、内部状态、速度 |
| `~/dynamic_obstacles` | 仅确认动态；TEB **独立订阅**（不进 `obstacles`） |
| TEB PathWaitGate | PATH 走廊相交且动态 → WAIT；超时或出话题 → 绕行 |
| local costmap 静态层 | 只接收确认静止的新增障碍（第一版可后做） |
| TEB《动态障碍物避让.md》 | 近场按当前位置保间距；不以速度做长时绕行；OmegaHold 不订本话题 |

最小实现顺序：

1. 地面分割 + 点级静图残差 + 残差聚类（能列出地图外障碍）。
2. `map` 系多线索关联 + Kalman + 状态机；发布 `~/dynamic_obstacles` 与 `~/markers`。
3. TEB 路径走廊求交 → WAIT / DETOUR / FOLLOW（改规划不改本包分类）。
4. 确认静止写入 local costmap 静态层。
5. 无图区域再补自由空间种子；不要一开始上完整 Dynablox。

## 验收场景

用下列场景标定阈值，而不是追点级 IoU：

| 场景 | 期望 |
|---|---|
| 仅静图内墙/柱挡路 | `dynamic_obstacles` 为空，直接绕，不停车 |
| 行人横穿前方路径 | 0.3–0.5 s 内黄→红并进入动态话题，停车；人离开后空数组再走 |
| 行人在路上站住 | `T_settle` 后红→绿，离开动态话题，改为绕行 |
| 纸箱放到路径上后不动 | 保持黄/绿，不进动态话题，允许绕（不要停死） |
| 人贴墙走 | 残差云里还有人体；红框跟上并停车，不从人身前绕 |
| 后方静物被前方遮挡后露出 | 保持黄/绿，编号可带 G，不进动态话题 |
| 定位回环跳变 | 不把整面墙判成动态导致停死 |
| 短暂遮挡丢检 | coast 期间 ID 与对外状态不变，不闪烁停/绕 |
| PATH 上动态挡路 | TEB WAIT，航向不变，FollowPath 不进 recovery |
| 等满 `path_wait_timeout` 人还在动 | DETOUR latch，同一 ID 不来回停/绕 |
| 邻行行人、走廊半宽合理 | 不停车 |

## 明确不做

- 不用原版 Dynablox 的点级动态标签直接驱动停车/绕行。
- 不用语义「这是人」作为停车的充分条件。
- 不用障碍速度驱动 TEB 长时绕行。
- 不在动态区把 TSDF/静图永久 `w=0`。
- 第一版不上无图自由空间补丁。
- 不把本包动态话题接到 TEB `obstacles`（独立订阅做 PATH 停车门控）。
- 不对整朵非地面点云先聚类再整簇判静图。
