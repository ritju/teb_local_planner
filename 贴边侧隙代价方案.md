# 贴边侧隙代价方案（讨论结论 + 落地技术设计）

> 分支：`lane_edge`  
> 日期：2026-08-06  
> 目的：固化讨论结论，并给出可直接改代码的落地技术方案。

---

## 1. 背景与问题

清洁机器人贴边目标：

- **边刷外缘**贴到马路牙子（清洁到位）；
- **硬质结构**不碰撞马路牙子（安全）。

实车几何（两侧外缘距离）：

| 部位 | 全宽 | 半宽（相对中心） |
|------|------|------------------|
| 边刷外缘 | 1.5 m | **0.75 m** |
| 硬质结构 | 1.3 m | **0.65 m** |
| 单侧刷毛外伸 | — | **0.10 m** |

### 1.1 旧方案问题

现网 `EdgeDistanceToWall` 用 **位姿中心 → 墙线垂距**，误差为：

```text
|d_center - min_wall_dist|
```

主要缺陷：

1. **矩形车体**：中心距达标时，前角侧隙仍可能过近或过远。
2. **`perpendicularDistance` 假零误差**：当 `d > distance_tolerance` 时返回 `min_wall_dist`，使 `_error=0`，远距失去拉力。
3. **双重表达冲突**：马路牙子同时是墙线（吸引）与深度点云障碍（`weight_obstacle`/`weight_inflation` 排斥），甜点被外推。
4. **膨胀项**：`inflation_dist` 往往远大于目标侧隙，在“已不碰撞”时仍持续外推。
5. **死区若令 Pull=0**：via / 时间最优等项主导，侧向易乱动。

### 1.2 目标语义（已对齐）

优化量改为：

```text
g = dist(矩形 footprint, 贴边参考几何)
```

- **Pull**：始终把 `g` 拉向目标侧隙 `g*`（钉住横向位置，避免乱动）。
- **Push**：仅当 `g` 过小、硬结构有碰撞风险时额外推远。
- **不要**在死区把 Pull 置 0。

`min_wall_dist`（约 0.75）仅保留为**进入贴边/中心距匹配**等逻辑，**不再作为优化误差目标**。

---

## 2. 讨论结论摘要

### 2.1 贴边与避障的关系

不是全局降低障碍代价，而是在「刷尖可贴、硬结构不碰」的姿态上，让障碍/膨胀代价接近 0，使贴边项能主导；过近时障碍/Push 必须变硬。

### 2.2 裁剪与 feasible

只松**墙带内路缘浅层**同源障碍；车头（前伸约 1.2 m）、凸出物、车辆不得裁掉。  
裁过猛 → TEB 轨迹擦 costmap → feasible 失败；footprint 过瘦 → 实车碰撞。

### 2.3 Pull / Push 分工（最终共识）

```text
e_pull = g - g*                         # 始终指向 g*
e_push = max(g_push - g, 0)             # 仅 g < g_push
```

| 区间 | Pull | Push | 说明 |
|------|------|------|------|
| `g > g*` | 拉近墙 | 0 | 清洁：把刷尖拉回去 |
| `g_push ≤ g ≤ g*` | 仍指向 `g*`（可略降权） | 0 | 边刷可接触带，靠 Pull 钉住，不乱动 |
| `g < g_push` | 仍可有、但权更小 | 强推远 | 硬结构保护 |

参数关系：

```text
g_push  <  g*  ≤  g_pull_cap（可选远距吸引上限）
```

示例（硬结构 footprint 半宽 0.65）：

| 参数 | 建议值 | 含义 |
|------|--------|------|
| `g*` (`desired_side_clearance`) | 0.08 ~ 0.10 | 硬结构到贴边参考面的目标侧隙 |
| `g_push` (`side_clearance_push`) | 0.04 ~ 0.06 | 再近则 Push |
| `side_clearance_attract_max` | 0.30 ~ 0.40 | `g - g*` 超过后不再加强吸引（可选） |
| `w_pull` | 80 | Pull 权重 |
| `w_push` | 200 | Push 权重（更大） |

消抖靠：**Push 阈值低于 `g*` + `w_push > w_pull`**，而不是 Pull 死区归零。

### 2.4 参考几何

优先使用与过滤一致的 **墙线 `LineObstacle`（eff_wall）** 作为侧隙参考面：

- 稳定、连续，适合沿墙；
- 与 `robot_model_->calculateDistance(pose, obst)` 复用同一距离定义（含矩形姿态）。

可选二期：墙带内最近多边形与墙线取 `min(g)`，轮廓偏大时更保守。

---

## 3. 落地技术方案

### 3.1 架构

```text
墙线提取 / 锁定
    → eff_wall（延长；safety_offset 建议 0）
    → ① 窄带裁剪路缘噪声（保留前方/凸出）
    → ② 注入 LineObstacle(eff_wall)，标记为 wall_guide
    → TEB：
         EdgeWallSideClearance  （Pull + Push，2 维误差或 2 条边）
         EdgeParallelToWall     （保留）
         EdgeObstacle/Inflation （跳过 wall_guide；其余障碍正常）
```

### 3.2 侧隙计算

```cpp
double g = robot_model_->calculateDistance(bandpt->pose(), wall_guide_obstacle);
```

与 `EdgeObstacle` 一致，自动处理多边形 footprint 变换与最短距。

### 3.3 代价边设计

**推荐新建** `EdgeWallSideClearance`（保留旧 `EdgeDistanceToWall` + 开关兼容）。

#### 方案 A：二维误差（推荐）

`BaseTebUnaryEdge<2, ...>`：

```text
_error[0] = g - g*;                      // Pull
_error[1] = max(g_push - g, 0);          // Push
information(0,0) = w_pull * k_traj;
information(1,1) = w_push * k_traj;
```

#### 方案 B：一维非对称

```text
_error[0] = g - g*;
// 若 g < g_push，对误差再放大 push_scale，或动态提高 information
```

Phase A 建议用方案 A，语义清晰、调参直观。

#### 必须修复

删除旧逻辑：

```cpp
// 禁止：超 distance_tolerance 时返回 min_wall_dist 制造假零误差
distance = (d <= tolerance) ? d : min_wall_dist;
```

远距策略改为：

- `_error[0] = 0` 当 `(g - g*) > attract_max`，或  
- 权重随距离衰减（沿用 `distanceWeightScale` / 侧隙调度）。

### 3.4 `AddEdgesDistanceToWall` 改造

文件：`teb_local_planner/src/optimal_planner.cpp`

1. 用当前 `wall_line_`（或与 ROS 层同步的 eff_wall）构造/缓存 `LineObstacle`。
2. 每个 pose 调用 `setParameters(cfg, robot_model_.get(), wall_guide)`。
3. 轨迹前向缩放保留：`EdgeDistanceToWall::distanceWeightScale`（可改名复用）。
4. **加边墙线必须与注入 obstacles 的墙线一致**（extension 一致；`safety_offset=0` 以免双边错位）。

### 3.5 墙线与普通障碍解耦

文件：`obstacles.h` / `teb_local_planner_ros.cpp` / `AddEdgesObstacles*`

1. 在 `Obstacle` 增加标记，例如：
   - `bool wall_guide_{false};`
   - `void setWallGuide(bool v); bool isWallGuide() const;`
2. 注入 eff_wall 的 `LineObstacle` 时 `setWallGuide(true)`。
3. `AddEdgesObstacles` / `Legacy`：`if (obst->isWallGuide()) continue;`
4. 侧隙边专门约束该 guide，避免与 `weight_obstacle`/`weight_inflation` 双重推墙。

### 3.6 参数表（配置层）

文件：`teb_config.h` / `teb_config.cpp` + 实车 yaml

| 参数名 | 类型 | 建议默认 | 说明 |
|--------|------|----------|------|
| `wall_side_clearance_mode` | bool | true | true=侧隙；false=旧中心距 |
| `desired_side_clearance` | double | 0.08 | \(g^\*\) |
| `side_clearance_push` | double | 0.05 | \(g_{\mathrm{push}}\)，须 `< g*` |
| `side_clearance_attract_max` | double | 0.35 | 超过则 Pull 误差清零或降权 |
| `weight_wall_side_pull` | double | 80.0 | Pull |
| `weight_wall_side_push` | double | 200.0 | Push |
| `min_wall_dist` | double | 0.75 | **仅**进模式/匹配，不进侧隙误差 |
| `edge_footprint_vertices` | string | 半宽 **0.65** | 硬结构外缘 |
| `edge_min_obstacle_dist` | double | 0.05 | 非 guide 障碍硬底 |
| `wall_line_obstacle_filter_distance` | double | 0.18 | 墙带裁剪 |
| `wall_line_obstacle_filter_distance_max` | double | 0.20 | 裁剪上限 |
| `wall_line_safety_offset` | double | 0.0 | 与侧隙参考对齐时建议 0 |

贴边模式动态建议：

- `weight_inflation` → 1.0（或更低）；
- 若可改：`inflation_dist` ≤ `g* + 0.05`；
- via：贴边时可降 `weight_viapoint`（0~0.2），减少全局路径内偏。

权重调度（可选改造 `computeWallLineDistWeightFromRobotDistance`）：

- 输入改为当前 `g` 或中心距；
- `g < g_push`：偏 Push；
- `g* < g < g*+attract_max`：Pull 满；
- 更远：Pull 衰减。

### 3.7 涉及文件清单

| 文件 | 改动 |
|------|------|
| `g2o_types/edge_wall_side_clearance.h`（新建） | Pull/Push 侧隙边 |
| `g2o_types/edge_distance_to_wall.h` | 保留旧逻辑；或内部委托新边；修假零误差 |
| `optimal_planner.cpp` / `.h` | `AddEdgesDistanceToWall`；注册 g2o 类型 |
| `obstacles.h` (+ 如有 .cpp) | `wall_guide` 标记 |
| `teb_local_planner_ros.cpp` | 注入 LineObstacle 时打标；与 eff_wall 一致 |
| `teb_config.h` / `teb_config.cpp` | 新参数声明/读取/动态更新 |
| 实车 nav2 yaml | 参数落地 |

### 3.8 实现分期

#### Phase A（最小可用）

1. 新建 `EdgeWallSideClearance`：`g = calculateDistance`，`e0=g-g*`，`e1=max(g_push-g,0)`。
2. `wall_side_clearance_mode` 开关；false 走旧边。
3. 墙线 `LineObstacle` 标 `wall_guide`，障碍边跳过。
4. 修假零误差；yaml：`g*=0.08`，`g_push=0.05`，footprint 半宽 0.65，filter 0.18，贴边降 inflation 权重。

#### Phase B

1. 轨迹前向 + 按 `g` 的权重调度。
2. `attract_max`；过近时 Pull 降权。
3. 调试话题/日志：每周期 `g`、`g*`、`g_push`、模式 pull/push。

#### Phase C（可选）

墙带最近多边形参与 `g = min(g_line, g_poly)`。

---

## 4. 实车参数落地表（刷 1.5 / 硬 1.3）

```yaml
FollowPath:
  wall_side_clearance_mode: true
  desired_side_clearance: 0.08
  side_clearance_push: 0.05
  side_clearance_attract_max: 0.35
  weight_wall_side_pull: 80.0
  weight_wall_side_push: 200.0

  min_wall_dist: 0.75                    # 仅匹配/进贴边
  distance_tolerance: 1.3

  edge_footprint_vertices: "[[1.15, 0.65], [1.15, -0.65], [-0.65, -0.65], [-0.65, 0.65]]"
  edge_min_obstacle_dist: 0.05

  wall_line_obstacle_filter_distance: 0.18
  wall_line_obstacle_filter_distance_max: 0.20
  wall_line_safety_offset: 0.0

  wall_line_dist_robot_weight_radius: 2.0
  wall_line_dist_weight_scale_at_robot: 0.3
  wall_line_dist_weight_scale_far: 1.0

  # 旧 weight_wall_line_dist 可映射为 pull，或废弃改用 weight_wall_side_pull
  weight_wall_line_dist: 80.0
  weight_inflation: 1.0                  # 贴边模式建议动态写入
```

几何核验：

| 中心距墙（平行时） | 硬结构侧隙 g | 刷尖侧隙 | 期望 |
|--------------------|--------------|----------|------|
| 0.75 | 0.10 | ≈0 | 接近 `g*≈0.08~0.10` 甜点 |
| &lt; 0.65+g_push | &lt; g_push | 刷已过盈 | Push 主导 |

---

## 5. 验收标准

| 场景 | 期望 |
|------|------|
| 直道贴边 | 稳态 `g ≈ g*`（±0.03 m） |
| 偏航 5°~10° | 前角侧隙不显著小于 `g_push` |
| 路缘轮廓偏大 3~5 cm | 车略外移，硬结构不撞，feasible 通过 |
| 前方凸出 | 不因墙带裁剪漏检；退出贴边或绕行 |
| 对比旧中心距模式 | 同 filter 下 feasible 失败减少，侧向更稳 |

调试日志（节流 2 s）：

```text
[WallSideClearance] g=0.12 g*=0.08 g_push=0.05 e_pull=0.04 e_push=0.00 w_pull=80 w_push=200
```

---

## 6. 风险与对策

| 风险 | 对策 |
|------|------|
| 与 EdgeObstacle 双重推墙线 | `wall_guide` 跳过普通障碍边 |
| inflation 仍外推 | 贴边降 `weight_inflation` / 缩小 `inflation_dist` |
| 多边形残渣顶开 | filter 深度与 `g*` 对齐（约 0.15~0.20） |
| feasible 失败 | 查车头/角点是否被走廊误裁 |
| Pull/Push 顶牛抖 | 保证 `g_push < g*` 且 `w_push > w_pull`；过近降 Pull |
| 开关兼容回归 | `wall_side_clearance_mode:=false` 可回退旧中心距 |

---

## 7. 后续改代码检查清单

- [x] `Obstacle` 增加 `wall_guide` 标记
- [x] 新建 `EdgeWallSideClearance`（2D：pull/push）
- [x] `AddEdgesDistanceToWall` 接入 `robot_model_` + guide `LineObstacle`
- [x] `AddEdgesObstacles*` 跳过 `wall_guide`
- [x] `teb_config` 新参数 + 动态更新
- [x] 注入 eff_wall 时 `setWallGuide(true)`，与优化用线一致
- [x] 删除/旁路假零误差逻辑
- [ ] 实车 yaml 按第 4 节更新（示例已写入 `teb_params.yaml`，实车 nav2 配置需同步）
- [ ] 贴边模式降 `weight_inflation`（及可选 via）
- [ ] 日志/`g` 可视化
- [ ] 直道 + 偏航 + 凸出 + 轮廓偏大 四场景验收
- [x] Phase A 编译通过（`colcon build --packages-select teb_local_planner`）

### Phase A 已改文件

| 文件 | 说明 |
|------|------|
| `obstacles.h` | `setWallGuide` / `isWallGuide` |
| `g2o_types/edge_wall_side_clearance.h` | 新建二维 Pull/Push 边 |
| `g2o_types/edge_distance_to_wall.h` | 修假零误差；远距 error=0 |
| `teb_config.h` / `teb_config.cpp` | 新参数默认值/声明/加载/动态更新/校验 |
| `optimal_planner.h` / `.cpp` | 注册边、加边、跳过 wall_guide、fallback LineObstacle |
| `teb_local_planner_ros.cpp` | 注入 eff_wall 时 `setWallGuide(true)` |
| `params/teb_params.yaml` | 示例参数 |

---

## 8. 一句话

用 footprint 到墙线 guide 的侧隙 `g` 替代中心距；**Pull 始终钉住 `g*`，Push 仅在 `g < g_push` 加强保护**；墙线从普通障碍边剥离，墙带只做窄裁剪，从而在矩形车体上安全贴边且不因死区归零而侧向乱动。
